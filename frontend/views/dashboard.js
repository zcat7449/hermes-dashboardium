/**
 * Dashboardium — modular entry point.
 * IIFE modules (no bundler required at runtime) share state through window.Dashboard.
 * Modules: config, utils, state, api, render, drag-drop, modal, actions.
 */
(function () { 'use strict'; })();
